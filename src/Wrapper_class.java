public class Wrapper_class {
    public static void main(String[] args) {
     // normally the primitives data types are not created as object but we if want to
        //create it as objects we can use wrapper class so they will be created as objects and we can also
        //apply many meathods on them
        Integer a = 16;
        // now it is created as object and we can also apply meathods to it

    }
}
